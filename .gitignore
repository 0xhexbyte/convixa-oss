# Dependencies
node_modules
.pnp
.pnp.js

# Build
.next
out
build
dist

# Env & secrets (never commit)
# .env.example and .env.example.docker are safe to commit (templates only)
.env
*.pem
credentials.json

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE
.idea
.vscode
.cursor
.cursor/mcp.json
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Drizzle (migration SQL + meta journal are versioned; see src/lib/db/README.md)

# Local SQLite (when using sqlite)
local.db
*.db

# Accidental paths from connection strings (never commit)
postgresql:/

# Documentation (optional: remove to track docs in repo)

# AI/Agents (local development scripts)
crewai-agents/
.cursorrules

# Twitter bot logs & analytics (local only)
scripts/twitter-bot/tweet-log.json
scripts/twitter-bot/.analytics.json
scripts/twitter-bot/.posted-tweets.json
scripts/twitter-bot/.launchd-out.log
scripts/twitter-bot/.launchd-err.log

# Build artifacts
tsconfig.tsbuildinfo
